<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Fragment.CollectionViewPagerFragment">
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/mainViewPager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="10">
        </androidx.viewpager2.widget.ViewPager2>

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/mainTabLayout"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:elevation="2dp"
            app:tabIndicatorColor="@color/custom_tabIndicatorColor"
            app:tabIndicatorHeight="1.2dp"
            app:tabMode="fixed"
            android:background="@color/custom_background"
            app:tabTextColor="@color/custom_textColor"
            app:tabSelectedTextColor="@color/custom_tabSelectedTextColor"
            app:tabTextAppearance="@style/CustomTextAppearance">
        </com.google.android.material.tabs.TabLayout>
</LinearLayout>